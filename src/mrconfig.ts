import { iMRConfig } from "@eos/mrsoft-core";

const Config: iMRConfig = {
    deloDevPath: "http://localhost/MRTemplates",
    devLogin: "tver",
    devPwd: "tver",
    useSvc: true,
    disableInfoLogs: true,
    dontShowNotificationsOnErrors: true
};

export default Config;
